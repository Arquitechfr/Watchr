{
  "name": "watchr",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm --filter backend lint && pnpm --filter mobile lint && pnpm --filter web lint",
    "test": "pnpm --filter backend test",
    "typecheck": "pnpm --filter backend typecheck && pnpm --filter mobile typecheck && pnpm --filter web typecheck",
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter mobile start\"",
    "mobile": "pnpm --filter mobile start",
    "web": "pnpm --filter web dev"
  },
  "devDependencies": {
    "eslint": "^9.5.0",
    "prettier": "^3.3.2",
    "typescript": "^5.5.2",
    "concurrently": "^8.2.2"
  }
}
